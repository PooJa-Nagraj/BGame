pipeline {
    agent any
    stages{
        stage('git checkout'){
            steps{
                sh 'echo passed'
                git branch: 'main', url: 'https://github.com/PooJa-Nagraj/BGame.git'
                
            }
        })

    }
}